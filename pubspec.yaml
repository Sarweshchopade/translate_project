name: project
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.0.0
  shared_preferences: ^2.2.2
  animated_widgets: ^1.1.0
  flutter_animate: ^4.0.0
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.5.0
  material_design_icons_flutter: ^7.0.7296
  url_launcher: ^6.2.5
  http: ^1.5.0
  # Translation and ML dependencies
  # tflite_flutter: ^0.10.4  # Temporarily disabled due to NDK issues
  # Speech dependencies
  speech_to_text: ^6.6.2
  flutter_tts: ^3.8.5
  permission_handler: ^11.3.1
  # Camera and image processing
  camera: ^0.10.5+5
  image_picker: ^1.0.7
  image: ^3.0.2
  # Android compatibility
  flutter_localizations:
    sdk: flutter
  # File and path handling
  path_provider: ^2.0.15
  path: ^1.8.3
  # UI enhancements
  flutter_staggered_animations: ^1.1.1
  lottie: ^2.7.0
  # Network and storage
  dio: ^5.3.2
  sqflite: ^2.2.8




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/carbon_credit.png